<?xml version="1.0"?>
<model>
  <name>nbv_probe_camera</name>
  <version>1.0</version>
  <sdf version="1.9">model.sdf</sdf>
  <description>
    Static probe camera for the viewpoint sweep. Same rgbd_camera settings as the
    ROV's nbv_camera so measurements transfer. Model origin = optical centre.
  </description>
</model>
